apiVersion: v1
kind: Service
metadata:
  name: cloudflare-tunnel-svc
  labels:
    app: cloudflare-tunnel
spec:
  ports:
  - port: 9898
    protocol: TCP
  selector:
    app.kubernetes.io/name: podinfo
