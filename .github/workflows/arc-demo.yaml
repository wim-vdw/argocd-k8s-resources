name: Actions Runner Controller Demo
on:
  workflow_dispatch:

jobs:
  Explore-GitHub-Actions:
    runs-on: arc-runner-set2-dockerindocker
    steps:
    - run: echo "🎉 This job runs on a runner scale set in my Kubernetes cluster!"
    - run: docker ps -a
